import styled from 'styled-components';

export const StyledButton = styled.button`
	all: unset;
	cursor: pointer;
	box-sizing: content-box;
	height: 1em;
	width: 2.7em;
	color: #800000;
	text-align: center;
`;
